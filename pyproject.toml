[build-system]
requires = ["hatchling>=1.26.0"]
build-backend = "hatchling.build"

[project]
name = "mini-moulinette"
version = "0.2.0"
description = "Python/Typer runner for mini-moulinette C piscine tests"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "rich>=13,<14",
  "typer>=0.12,<1.0",
]

[project.scripts]
mini-moul = "mini_moul_app.cli:cli"

[tool.hatch.build.targets.wheel]
include = [
  "mini_moul.py",
  "mini_moul_app/**/*.py",
  "mini-moul/**",
]

[tool.hatch.build.targets.sdist]
include = [
  "mini_moul.py",
  "mini_moul_app/**/*.py",
  "mini-moul/**",
  "README.md",
  "LICENSE",
  "MIGRATION_TO_PYTHON.md",
  "pyproject.toml",
]
